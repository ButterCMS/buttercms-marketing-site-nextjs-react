{
  "version": 2,
  "builds": [{
    "src": "package.json",
    "use": "@now/static-build",
    "config": { "distDir": "out" }
  }],
  "build": {
    "env": {
      "BUTTER_CMS_API_KEY": "@butter-cms-api-key"
    }
  }
}
